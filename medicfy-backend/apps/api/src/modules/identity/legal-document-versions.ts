// Placeholder versions. Per spec §15.2, the real privacy notice and
// terms are legal deliverables not yet commissioned ("Fase 1", gated
// before any real patient data). Replace these when counsel delivers
// the final documents — every consent row stores the version active
// at the moment of consent (M1-RN-004), so changing this value is
// exactly what triggers re-acceptance on next login.
export const CURRENT_PRIVACY_NOTICE_VERSION = "draft-2026-08-13";
export const CURRENT_TERMS_VERSION = "draft-2026-08-13";
